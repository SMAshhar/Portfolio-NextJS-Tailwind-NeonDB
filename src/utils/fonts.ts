import { Inter, Jura, Calligraffitti } from 'next/font/google'
 
export const inter = Inter({
  subsets: ['latin'],
  display: 'swap',
})
 
export const jura = Jura({
  subsets: ['latin'],
  display: 'swap',
})

export const cal = Calligraffitti({
  weight:'400',
  subsets: ['latin'],
})

